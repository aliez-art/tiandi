# 天地熔炉 Tiandi Furnace · 全量代码审查报告

> 审查方式：全量静态审查 + 工具链实测 + 5 路并行深度审查（core/state、server、recipe/dataset、UI/UX、engine/cli/app），所有关键结论均经人工逐条复核（含真实运行产物 `.kernel-ws/panic.log` 事故现场分析）。
> 实测工具：cargo clippy / cargo test / cargo fmt / cargo machete / cargo audit / npm audit / tsc -b + vite build / git 历史分析。
> 审查范围：全部源码（Rust ≈ 8.3k 行、TS/TSX ≈ 1.9k 行、Python 资产 ≈ 700 行）、配置、脚本、文档、仓库卫生。**未修改任何文件。**

---

## 0. 工具链实测结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| `cargo clippy --workspace --all-targets` | ✅ **0 警告** | 质量门真实有效 |
| `cargo test --workspace` | ✅ **74 测试全绿** | README 声称 31（M0 旧数）、roadmap 声称 70，均过时 |
| `cargo fmt --check` | ❌ 未通过 | `crates/tiandi-cli/src/kernel.rs:203`、`crates/tiandi-state/src/repos.rs:299` 等未格式化 |
| `cargo machete`（未使用依赖） | ⚠ 8 项 | 其中 `kamadak-exif` 为误报（scan.rs:237 实际使用），其余 7 项属实（§2.3） |
| `cargo audit` | ✅ 0 漏洞 | 18 条 warning：glib 0.18.5 unsound、gtk-rs/atk/unic-* unmaintained，均为 tauri/rfd 链的 Linux 路径传递依赖，Windows 构建不编译，低危 |
| `npm audit` | ✅ 0 漏洞 | |
| `tsc -b && vite build` | ✅ 通过 | strict + noUnusedLocals 开启 |
| 依赖规模 | 620 包 | 全部 crates.io，0 git 依赖；react/react-dom + 少量构建链 |

**结论**：工程基础质量良好——编译零警告、74 测试全绿、无已知 CVE。问题集中在**逻辑缺陷、安全面、死代码与文档漂移**，见下文。

---

## 1. 目录结构全解（每个文件夹的作用）

### 1.1 仓库根目录

| 路径 | 作用 | 备注 / 风险 |
|---|---|---|
| `Cargo.toml` | Rust workspace 清单：10 个 crate + `workspace.dependencies` 统一版本 | — |
| `Cargo.lock` | 620 包依赖锁定（已提交，二进制工作区标准做法）✅ | — |
| `PRD.md` | 产品需求文档 v1.1（ADR-001 架构决策 + 三参考项目审查附录） | 与代码有漂移（§6） |
| `README.md` | 项目说明、快速开始、仓库布局 | **严重过时**：写"M0 完成，M1 未开始"，实际 M1/M2/M3 全部完成（roadmap 与 git log 证实） |
| `docs/` | `architecture.md`（技术架构）/ `roadmap.md`（里程碑）/ `model-support.md`（模型矩阵）/ `使用指南.md`（用户手册） | 使用指南与 UI 严重漂移（§6） |
| `crates/` | 10 个 Rust crate（§1.2） | — |
| `ui/` | 前端（React 18 + Vite + TS，§1.3） | `dist/`、`node_modules/` 已 gitignore |
| `start.bat` | 一键点火：定位 tiandi.exe → 校验 `.kernel-ws\tiandi.db` → 端口检测 → 打开服务窗口 → 健康轮询 | **GBK/ANSI 编码**（与全仓 UTF-8 不一致），UTF-8 编辑器/终端下乱码 |
| `server_window.bat` | 服务日志窗口（start.bat 内部调用） | **GBK 编码 + 首行 `chcp 65001` 自相矛盾**：切到 UTF-8 代码页后自身中文输出全部乱码 |
| `测试底模/` | **72.7 GB** 用户底模（anima/krea2/noobai/illusion 等 11 个 .safetensors） | 文件类型被 `*.safetensors` 忽略，但**目录本身无 .gitignore 条目**；磁盘占用巨大 |
| `测试数据集/` | 21 MB 测试图 + caption（旧 `1_tag/` 已在 git 历史中，新 `tag/` 未跟踪） | **6 张 PNG（约 21 MB）已提交进 git 历史**（commit c7acbb5），仓库卫生问题（§7） |
| `.kernel-ws/` | **10.6 GB** 实际运行工作区（§1.4）：训练内核、模型、数据、产物、DB、事故现场 | 已 gitignore；是"真实数据目录"放在仓库内 |
| `.kernel-install/` | 内核安装中间产物与日志（install.log / model-dl.log 等） | 已 gitignore，可随时清理 |
| `target/` | **18.6 GB** Rust 构建产物 | 已 gitignore |

### 1.2 crates/（10 个 Rust crate）

| crate | 作用 | 规模 | 关键发现 |
|---|---|---|---|
| `tiandi-core` | 领域模型（Project/BaseModel/Dataset/Recipe/Run/Checkpoint/MetricPoint）、RunState 状态机、EventBus（tokio broadcast） | 537 行 | `RunStateError` 死代码；Failed 终态与重试路径矛盾（P1） |
| `tiandi-state` | SQLite（rusqlite bundled）迁移 + 仓储 + RunManifest 落盘 | 1,100 行 | **P0 v3 迁移非幂等**；RunManifest 从未接线（死代码）；chrono 依赖未用 |
| `tiandi-dataset` | 图像管线：递归扫描、dHash 去重、EXIF、分辨率分桶、rayon 缩略图 | 706 行 | **P0 缩略图并发写竞争**；dHash 全零误判；EXIF Orientation 未应用 |
| `tiandi-recipe` | 丹方 schema（serde）、校验器、4 内置预设、TOML 读写 | 906 行 | 未知键不拒绝；3 处死代码；serde_json 依赖错位 |
| `tiandi-engine` | `Trainer` trait + TrainJob + EngineInfo | 103 行 | pause/resume/query 在 trait 中定义但实现层全部是空壳 |
| `tiandi-engine-compat` | **内核桥**：kernel_runner.py 编排、TOML/YAML 参数映射、IPC JSON Lines 解析、进程监督 | 1,759 行 | **P0 batch_size 静默丢失**；Mutex poison 已真实 panic 过；心跳/握手/卡死检测未实现 |
| `tiandi-engine-native` | candle 原生训练内核（远期探索） | 4 行占位 | 空占位，符合 PRD 决策 ✅ |
| `tiandi-server` | axum REST + SSE + 静态资源 + 任务队列调度 + 任务监督 + 端口回退 | 2,206 行 | **P0 CORS 全放行**；**P0 run_logs 路径遍历**；多个端点/依赖死代码 |
| `tiandi-cli` | `tiandi init/doctor/kernel/models/server` | 870 行 | `init --name` 参数被忽略（伪功能）；`--web` 浏览器端口探测不完整 |
| `tiandi-app` | Tauri 2 桌面壳（内嵌 server + WebView） | 77 行 + 配置 | **CSP 为 null**；`demo: true` 硬编码；capabilities 权限最小化 ✅ |

### 1.3 ui/（前端）

| 路径 | 作用 |
|---|---|
| `src/App.tsx`（278 行） | 布局壳：左侧导航（LoRA 训练 / 全量训练 / 炼丹记录）+ 右侧主区；GPU 3s 轮询；SSE 订阅；端口发现重试 |
| `src/components/TrainSetup.tsx`（936 行） | **最大文件**：丹方表单（家族/参数面板/模型 VAE TE 选择/数据集选择/保存/点火/载入/删除） |
| `src/components/Console.tsx`（317 行） | 训练控制台：进度环、loss/lr SVG 曲线、日志流、采样画廊/药库、续丹/熄灭 |
| `src/api.ts`（347 行） | API 客户端 + 端口发现（18765–18774）+ SSE 订阅 |
| `src/index.css` | 深色琉璃主题（朱砂红/琥珀金/青瓷青，与 PRD §9.1 一致） |
| `src/main.tsx` / `vite-env.d.ts` / `index.html` | 入口 |
| `dist/` | 构建产物（由 server 托管，`index-*.js/css` 已构建） |
| `package.json` / `tsconfig.json` / `vite.config.ts` | react 18.3.1 + vite 6；strict；dev proxy → 18765 |

### 1.4 .kernel-ws/ 内部（运行时数据目录）

| 路径 | 作用 |
|---|---|
| `.kernel/` | sd-scripts 内核：venv（Python+torch）+ sd-scripts 源码 + `kernel.json` 内核清单 |
| `.kernel-aitk/` | ai-toolkit 内核（Krea 2 后端）：独立 venv + ai-toolkit 源码 |
| `models/` | Krea 2 离线转换资产（qwen3vl_4b TE 目录、vae diffusers 目录） |
| `datasets/` | UI 注册的数据集记录 |
| `output/` | 产物静态根（`/output/` URL 映射；示例图 + 每轮 LoRA） |
| `recipes/` | 丹方 TOML 文件目录 |
| `runs/` | 任务目录（`runs/<任务ID>/logs/`、`checkpoints/` 产物、采样图） |
| `vault/` | 药库（产物重命名/归档区） |
| `tokenizers/` | 离线 tokenizer（CLIP-L/G，SDXL 用） |
| `tiandi.db` | 主数据库（SQLite：任务/指标/产物/设置） |
| 根下 13 个一次性脚本（`*.py`、`vae_target_keys.json`） | 资产转换/验证的临时脚本（convert_vae、dump_*、verify_*、mmdit_ref 等），**无版本管理** |
| `panic.log` | **真实 panic 事故现场**：`PoisonError`（Mutex 中毒）回溯——见 §3.2 |

---

## 2. 死代码与未引用（全部经工具 + 人工双重确认）

### 2.1 前端（api.ts 定义但 0 处 UI 调用）

| 函数 | 位置 | 对应后端端点 |
|---|---|---|
| `listPresets` | api.ts:169-173 | `/api/recipes/presets` |
| `scanDataset` | api.ts:221-232 | `/api/datasets/{id}/scan` |
| `deleteDataset` | api.ts:234-237 | `DELETE /api/datasets/{id}` |
| `fetchSettings` / `updateSettings` | api.ts:281-295 | `/api/settings` GET/PUT |
| 类型 `PresetView` | api.ts:155-161 | 仅被 listPresets 引用 |

> 即使用指南承诺的"预设套用、扫描、数据集删除、炉房设置页"在 UI 中均不存在（§6）。

### 2.2 后端：注册但无任何客户端/调用方的端点

| 端点 | 位置 | 说明 |
|---|---|---|
| `GET/POST /api/projects` | api.rs:63 | **Project 领域概念整体僵尸化**：UI 从不传 project_id，无任何页面使用 |
| `POST /api/runs/{id}/transition` | api.rs:69 | 仅测试使用（api.rs:382-409） |
| `GET /api/datasets/{id}/images`、`/buckets` | datasets.rs:28-29 | 桶分布已由 scan 报告带出，两端点无人用 |
| `GET /api/recipes/validate` | recipes.rs:23 | 前端不走此端点 |
| `GET /api/checkpoints`（list_all）、`GET /api/checkpoints/{id}`（get_one） | vault.rs:18-20 | 前端只用 list_by_run |
| `GET /api/settings`、`PUT /api/settings`、`GET /api/recipes/presets` | system.rs/recipes.rs | 客户端函数存在但无 UI 调用（2.1） |

### 2.3 未使用依赖（cargo machete 实测，7 项属实）

| 位置 | 依赖 | 备注 |
|---|---|---|
| tiandi-app/Cargo.toml:14-15 | serde、serde_json | Tauri 无自定义 command，无需序列化 |
| tiandi-engine-compat/Cargo.toml | chrono、serde、tiandi-state | 全 crate 无引用 |
| tiandi-server/Cargo.toml:17 | regex | 全 crate 无引用 |
| tiandi-state/Cargo.toml:13 | chrono | 全 crate 无引用（uuid 仅测试用，应移 dev-dependencies） |
| ~~tiandi-dataset~~ | ~~kamadak-exif~~ | **误报**：scan.rs:237 `use exif::` 实际使用，勿删 |

### 2.4 Rust 死代码项

| 项 | 位置 | 说明 |
|---|---|---|
| `RunStateError` / `RunStateError::new` | core/state.rs:102-113 | 全 workspace 无调用方；迁移校验被调用方绕过（api.rs:185 用 matches!） |
| `RunManifest` / `ManifestError` | state/manifest.rs:11-58 | 无生产调用方；PRD 承诺的 manifest.json 崩溃恢复链路从未接线 |
| `Store::conn()` | state/repos.rs:68-70 | 无调用方 |
| `ModelFamily::backend()` | core/domain.rs:32-37 | 仅测试用；真实路由是 compat lib.rs:278-284 独立 match |
| `Run::manifest_path` 字段 | core/domain.rs:158,179 | 恒为 None，永不写入 |
| `RecipeData::family_supported` | recipe/schema.rs:296-303 | 无调用方且恒为 true（ModelFamily 仅 3 变体） |
| `OptimizerKind::label` / `PredictionType::label` | recipe/schema.rs:29-43,133-141 | 无调用方（映射层用 `optimizer_type()`） |
| `ScanError::new` | dataset/scan.rs:268-272 | 无调用方 |
| `SdScriptsTrainer::ready/env_message/kernel_env` | compat lib.rs:50-61 | 无生产调用方（grep 证实） |
| `TrainPaths.anima_qwen3_tokenizer` | compat lib.rs:236、toml_map.rs:545 | 两处构造均硬编码 None，toml_map.rs:46/328 消费分支恒不生效 |
| `Krea2Assets.mmdit` | compat kernel.rs:335 | 解析后从未被 kernel_launch 使用（只存不用） |
| kernel_runner.py `--mock` 参数分支 | kernel_runner.py:344 | Rust 侧从不传 `--mock`（只传 env TIANDI_KERNEL_MODE）——死分支 |
| `seed_tokenizers.py` | assets/ | 无任何 Rust 调用方（tokenizer 种入靠手工/文档） |
| 空壳 lib.rs | engine-native/cli/app | 各 4 行 re-export 占位（engine-native 符合 PRD；cli/app 的属冗余） |
| `sse::event_run_id`、`queue::build_job/fail_run`、`api::router`、`lib::output_root` | server | 均无 crate 外调用方，应降 `pub(crate)` |
| CSS 死规则整块 | ui/index.css:43-52, 229-301, 304-371 | 药材视图/弹窗/firebar/single-layout 全部无对应 JSX；`.run-del:hover` 重复定义 |
| `STATE_LABEL` 双份 | App.tsx:18-29 与 Console.tsx:34-45 | 完全相同定义两遍，状态机新增状态需改两处 |
| CLI `init --name` | cli/main.rs:217-219 | 参数接收后只打印"项目注册（M1 实现）"，实际忽略 |

---

## 3. 后端问题清单（按严重度，全部带证据）

### 🔴 P0（必须立即修）

**P0-1 SQLite v3 迁移非幂等 → 数据库可能永久无法打开**
`crates/tiandi-state/src/lib.rs:117-124`：`ALTER TABLE runs ADD COLUMN base_model_id TEXT` 无存在性保护，且 `migrate()`（32-138）未包事务（execute_batch 逐句自动提交）。若在 ALTER 之后、`pragma_update(user_version,3)` 之前崩溃/断电，重启后重跑 ALTER → "duplicate column name" → **库永远打不开**。v1/v2/v4 的 `CREATE TABLE IF NOT EXISTS` 均幂等，唯独 v3 不是。现有测试（lib.rs:154-162）只测"成功后重跑"，不覆盖崩溃窗口。
修复：迁移整体包事务；v3 改为 `PRAGMA table_info(runs)` 条件 ALTER。

**P0-2 CORS 全放行 → 任意网页可驱动本地服务（读/写/删/拉起训练）**
`crates/tiandi-server/src/lib.rs:63` `.layer(CorsLayer::permissive())`，服务只绑 127.0.0.1 但**无 Origin 校验、无 token**。这是经典 localhost CSRF 面：用户浏览恶意网页时，该页 JS 可
- 读：`/api/runs`、`/api/runs/{id}/logs`、`/api/system`（GPU 信息）、SSE 事件流；
- 写：`DELETE /api/runs/{id}`（删记录+产物目录）、`DELETE /api/checkpoints/{id}`（**删文件**）、`rename`（移动文件）、`POST /api/runs` + `/start`（拉起训练）、`/cancel`（中断训练）、`POST /api/datasets/{id}/scan`（CPU/IO DoS + 目录结构泄露）、`POST /api/import-asset`（任意文件复制进工作区）、`POST /api/pick-file|pick-dir`（弹原生对话框并把路径回传）。
无 JSON 体的 POST 连预检都不需要。lib.rs:55-57 注释"permissive CORS 无风险"的判断是**错的**。
修复：Origin 白名单（`tauri://localhost`、`http://localhost:*`、`http://127.0.0.1:*`）+ 校验 Origin/Host，或一次性 CSRF token。

**P0-3 run_logs 路径遍历 → 任意文件尾 300 行可读**
`crates/tiandi-server/src/api/vault.rs:27-47`：`runs_dir.join(&run_id).join("logs/training.log")`——run_id 直接来自 URL 且**未校验 run 存在、未规范化**。axum Path 会做百分号解码，`/api/runs/..%2F..%2F..%2FWindows%2Fwin.ini/logs` 之类可读任意 `<目录>/logs/training.log`；绝对路径 run_id 可直接覆盖基目录。对比 `delete_run`/`list_by_run` 都有 `get_run` 前置校验，唯独这里漏了。
修复：handler 内先 `get_run`，再 canonicalize + `starts_with(runs_dir)` 断言。

**P0-4 batch_size 在 sd-scripts 映射中静默丢失（训练配置错误）**
丹方 schema.rs:159 定义、validate.rs:122-128 校验 `[1,64]`，但 `build_sdscripts_toml` / `build_sdscripts_toml_full`（toml_map.rs）与 kernel_runner.py **均不输出 batch_size**（grep 全仓仅 yaml_map.rs:82 消费）。→ SDXL/Anima 训练时用户配置被校验器放行后**静默丢弃**，sd-scripts 用默认值（通常 1）。这是"校验器放行 → 映射层丢弃"的典型漏网路径，用户完全无感知。
修复：toml_map.rs 输出 batch_size + 加映射测试。

**P0-5 缩略图并行写竞争（TOCTOU）→ 文件撕裂**
`crates/tiandi-dataset/src/scan.rs:142-144`：`if !dest.exists() { make_thumbnail(...) }` 检查与写入非原子；par_iter（scan.rs:130-150）中两张同哈希图并发写同一 `thumbs/{hash}.jpg`，`image` 库直接写目标文件（thumb.rs:21，无 temp+rename）→ 文件撕裂。数据集含重复图时**必然触发**。附带：全零哈希图共享 `thumbs/0000000000000000.jpg`，后续所有纯色图的 thumb 字段都指向第一张的缩略图。
修复：temp + rename 原子写。

**P0-6 Anima T-LoRA 映射到不存在的网络模块 → 训练必崩**
`toml_map.rs:60-62`：`DitAnima + NetworkType::Tlora` 映射 `network_module = "networks.tlora_anima"`；但**已安装的 sd-scripts（锁定 commit 068bcd7）`networks/` 目录实测无 `tlora_anima.py`**（仅有 `lora_anima.py`、`control_net_lllite_anima.py` 等）。选 T-LoRA 的 Anima 训练在内核 import 阶段必然失败。且 roadmap.md:44 自己写"T-LoRA 走 lora_anima"——代码与 roadmap 自相矛盾。修复：映射改为 `lora_anima`（或确认上游模块名后修正并加映射测试）。

**P0-7 ai-toolkit `disable_sampling` 写错配置层级 → 空 prompt 采样**
`yaml_map.rs:169-171`：无采样 prompt 时把 `disable_sampling: true` 放在 `sample:` 段下；但 ai-toolkit 中该键是 **train 级**（实测安装源码 `flux_train_ui.py:199/210` 写 `...process[0]["train"]["disable_sampling"]`，消费处 `SDTrainer.py:143` 读 `self.train_config.disable_sampling`）。→ 键被忽略：无采样 prompt 时**按默认每 100 步用空 prompt 采样**（浪费时间且可能出错）。修复：移到 train 段 + 对照 ai-toolkit config_modules 实测。

**P0-8 krea2_prepare.py 中文路径乱码 → Krea2 训练找不到模型（当前环境已处于坏状态）**
`krea2_prepare.py` 用 `print()` 输出清单（stdout），中文 Windows 默认 GBK 编码；Rust 侧按 UTF-8 解析（cli/kernel.rs:341-348 `String::from_utf8_lossy`）→ 中文路径变乱码。**实测当前 `.kernel-ws/.kernel/kernel.json`：`"mmdit": "D:/Projects/tiandi/���Ե�ģ/krea2/krea2_raw_bf16.safetensors"`**——用户实际目录"测试底模"（中文）已损坏，Krea2 训练必然找不到模型。修复：同 kernel_runner.py 用 `sys.stdout.buffer.write(utf-8)` 直写；或 Rust 侧 GBK 解码兜底。

### 🟠 P1（应该尽快修）

**P1-1 Mutex poison 已真实 panic（事故现场有据）**
`.kernel-ws/panic.log` 记录了真实 panic：`tiandi-engine-compat/src/lib.rs:279` `handles.lock().unwrap()` → `PoisonError`（某线程持锁 panic 后，另一线程 unwrap 中毒锁）。同模式还有 lib.rs:334、:341、:410 的 `lock().unwrap()`。panic hook 能落盘 panic.log（好设计），但服务线程可能因此死掉。修复：`unwrap_or_else(|e| e.into_inner())` 或处理 poison。

**P1-2 队列"拉起"与状态翻转竞态 → 同任务重复 spawn / 双任务并发**
`queue.rs:27-67`：pump 选出 Queued 后直接 `trainer.start`，**不置状态、无内存"starting"守卫**；状态要等内核 hello 事件才由 supervisor 置 Preparing（supervisor.rs:53-56）。真实 sd-scripts 冷启动（Python 解释器 + accelerate）可能超过 2s 轮询间隔 → 下一 tick 再次选中同一 run 再次 spawn。跨进程放大：端口回退明示支持多实例共享同一 DB（api.ts 探测 18765-18774），`has_running_run` 非原子 → 双实例同时拉起双训练。修复：start 前同步置 Preparing + DB 事务/文件锁原子认领。

**P1-3 cancel 语义缺陷（排队任务取消失败 + 取消后显示"炸炉"）**
- `api.rs:202-222`：Queued 未启动任务无句柄 → `UnknownRun` → 409，而 UI 对 queued 状态显示"熄灭"按钮 → **用户取消排队任务必然失败**；
- taskkill 强杀后 stdout EOF 触发兜底 fail 事件（kernel.rs:147-154）→ supervisor 置 Failed，与 cancel 置 Canceled 竞态，最后写库者胜 → **主动取消的任务常显示"炸炉"**。
修复：Queued 直接置 Canceled；EOF 兜底 fail 不覆盖 Canceled。

**P1-4 崩溃恢复遗漏 Paused + 孤儿内核进程**
- `recover_interrupted`（queue.rs:221-224）只覆盖 Preparing/Running/Sampling/Saving；`has_running_run` 把 paused 计为运行中 → 崩溃时存在 Paused 任务则**重启后队列永久阻塞（软死锁）**；
- kill 用 `taskkill /T /F`（kernel.rs:54-65），**非架构承诺的 Job Object**；`serve()` graceful shutdown 不杀 handles；崩溃后 sd-scripts 继续占 GPU，重启"续丹"会**再拉一个新内核 → 同一任务双进程训练**；启动时不清理残留 python。
- 恢复窗口遗漏"Queued 但内核已拉起"（P1-2 窗口）与孤儿进程叠加成双训练。

**P1-5 状态机事实源分裂（Failed 终态 vs 重试路径 vs 文档三方矛盾）**
`state.rs:85` 将 Failed 定为终态、`is_terminal()` 含 Failed、测试显式断言 `!Failed.can_transition_to(Queued)`（state.rs:148-152）；但 `api.rs:185` `enqueue_run` 允许 `Created | Failed → Queued`（重试路径），`architecture.md:58` 也写 `failed → queued（重试）`。三处互相矛盾，`/transition` 拒绝 Failed→Queued 而 `/enqueue` 放行。

**P1-6 broadcast 滞后静默丢事件 → 任务可能卡 Running**
容量 1024（events.rs:87）；supervisor.rs:27 `Lagged => continue`、sse.rs:40 跳过。supervisor 在 `scan_lora_artifacts` 长锁期间（持 store 锁递归扫盘+逐文件查询，supervisor.rs:46,106,221-225）或 UI 断连时，progress 洪峰可挤掉 **Done/Fail/RunStateChanged** → 任务永远卡 Running。且 supervisor 持全局 store 锁做文件 IO 会阻塞全部 API。

**P1-7 manifest 崩溃恢复链路整体未接线**
RunManifest 无生产调用方（PRD §8.5/architecture §3 承诺的"运行清单落盘 + 崩溃恢复"实际是 DB 状态扫描，queue.rs:214-238）；manifest.rs:38-44 原子写无 `sync_all()`、无目录 fsync、rename 失败 tmp 残留不清理、不建父目录；RunManifest 缺 `base_model_id` 字段。

**P1-8 旧格式 state 对队列查询不可见**
repos.rs:40-46 `parse_run_state` 兼容旧格式（JSON 带引号 `"queued"`），但 `list_queued_runs`（repos.rs:470）/`has_running_run`（repos.rs:492）只匹配新格式字面量 → 旧版本写入的行被调度器**完全跳过**。

**P1-9 dHash 去重正确性缺陷（误判 + 漏检 + 非传递）**
- 全零哈希：hash.rs:18-22 只编码"右比左亮"上升沿 → 纯色/水平渐变/横幅图哈希全零 → `hamming(0,0)=0` → **纯白与纯黑图被判为重复**（hash.rs:50）；
- 前缀分桶漏检：hash.rs:33 注释"仅同前缀才可能距离≤阈值"**断言错误**（高 16 位差 1 位、低 48 位相同 → 距离 1 却在不同桶永不比较）；
- 贪心星形聚类非传递：A~B、B~C、A!~C 时 C 漏检，组内两两距离可超阈值。

**P1-10 EXIF Orientation 未应用**
scan.rs:242-249 读取 Orientation 但从不旋转：宽高（scan.rs:137）、dHash（138）、缩略图（thumb.rs）全基于未旋转像素 → 手机竖拍照片分错桶、缩略图侧躺。

**P1-11 未知键不拒绝（丹方静默吞错）**
schema.rs:145、file.rs:10/33 无 `deny_unknown_fields` → `[data] learing_rate = 0.001` 拼写错误被静默吞掉，训练用默认值；与 architecture.md:124"未知键…校验"直接矛盾。

**P1-12 Krea2（ai-toolkit）映射硬编码，预设参数静默失效**
yaml_map.rs:96 `optimizer:'adamw8bit'`（预设用 AdamW）、:131 sampler `flowmatch`（预设 euler 失效）、:72 `cache_latents_to_disk:true`、:166 `seed:42` 均忽略丹方字段 → Krea2 预设的 optimizer/lr_scheduler/sample_sampler/seed **全部不生效**。

**P1-13 心跳/握手/卡死检测未实现（架构承诺缺失）**
Event 枚举（events.rs:13-60）无 Heartbeat 变体；kernel.rs:235 `_ => None` 静默丢弃未知类型；kernel_runner.py 从不发 heartbeat；无 30s 看门狗、无 hello 10s 握手校验（architecture.md §5.4 承诺）。`Trainer::query` 返回 Ok(None)（compat lib.rs:426-430）；`pause`/`resume` 返回 Unsupported（394-407）——PRD FR-504 与状态机的 Paused 态均无对应实现。

**P1-14 产物操作无根目录约束（纵深防御缺口）**
supervisor.rs:129-134 `rel_output_path` 在 `strip_prefix` 失败时**回退原始路径**、:71-79 `abs_output_path` 对内核给的绝对路径直接采用 → cp.path 被污染（含 `..`/绝对路径）时，`delete_one`/`rename_one`（vault.rs:83-86,112-133）可对 output_root 之外的文件删除/重命名。另 S4：丹方名未校验路径分隔符（recipes.rs:54-84 与 vault rename 校验不一致）→ 产物可写出输出目录（`..\..\evil`）。

**P1-15 run_logs 整文件读入内存 + 日志无限追加无旋转**
vault.rs:36-47 `read_to_string` 读整个 training.log；supervisor.rs:88-102 无限追加。数小时训练日志可达 GB 级 → 一次请求内存暴涨（与 P0-3 同源）；PRD NFR"日志滚动不撑爆磁盘"未落实；FR-802 磁盘余量展示未实现（system.rs 只有 GPU）。

**P1-16 解码无上限（解压炸弹）+ 每文件解码两次**
scan.rs:133 `image::open` 全量解码无尺寸上限，par_iter 并发（=逻辑核数）；每文件解码 2 次（133 + thumb.rs:10）+ 读 3 次（+EXIF 238）。多核 + 高分辨率数据集 → 瞬时内存数 GB；恶意声明巨幅 PNG 可 OOM。

**P1-17 前端交互缺陷组（详见 §4）**
自动命名 stale closure 必失败、保存丹方删旧建新丢数据、改参点火被静默忽略、失败原因不可见、SSE 事件风暴。

**P1-18 真实训练（sd-scripts/aitk）从不产生 sample 事件 → 采样画廊仅 mock 生效**
kernel_runner.py 的 `run_sdscripts`/`run_aitk`（95-176、244-317 行）只转发原始日志并解析 progress/metric，**从不 emit `{"type":"sample"}`**（仅 run_mock 发，85-89 行）；架构文档 §5.2 承诺的训练中采样画廊在真实训练中不会出现，UI 只会看到 mock 的占位采样图。修复：内核侧采样后补发 sample 事件（或 Rust 侧文件监控兜底——同样未实现）。

**P1-19 资产路径依赖 CARGO_MANIFEST_DIR → 二进制脱离源码树后训练无法启动**
`server/lib.rs:142-145`（default_wrapper_path）与 `cli/kernel.rs:315-318`（krea2_prepare.py 路径）均用 `env!("CARGO_MANIFEST_DIR")` 拼相对路径；`cargo install` 或拷贝二进制到别处后 wrapper/脚本不存在 → 训练启动失败（当前靠仓库内运行掩盖）。修复：随二进制嵌入（include_str! + 运行时落盘）或安装时写入可配置路径。

**P1-20 CLI 工作区不 canonicalize + Tauri 壳与浏览器模式工作区不一致**
- `cli/main.rs:185-191` resolve_dir 返回相对路径 → 内核 cwd 与配置中的相对路径错位（真实训练在多处用 `to_string_lossy` 拼路径）；
- Tauri 壳用系统 `app_data_dir`（app/main.rs:21），浏览器模式用仓库内 `.kernel-ws`（server_window.bat）——**两套互不相通的工作区**，同一模型/数据集/产物分处两地，桌面壳与浏览器模式体验割裂。

### 🟡 P2（建议）

- 删除多语句无事务（delete_dataset/run/recipe，repos.rs:266-396）+ 无 FOREIGN KEY；无 busy_timeout/WAL（CLI 与 server 同开一 DB 会撞 SQLITE_BUSY）
- `parse_run_state`（repos.rs:40-46）对非法/损坏 state 串静默降级为 Created（掩盖数据损坏，坏任务可能重新入队）
- 版本戳不一致：v4 分支写 `SCHEMA_VERSION`（lib.rs:136）而 v1-v3 写字面量（93/115/124）——SCHEMA_VERSION 升 5 时存在跳过 v5 变更的脆弱模式
- `unchecked_transaction`（repos.rs:685）可掩盖嵌套事务误用；settings 明文存储（现仅镜像/主题等非敏感值，未来存 HF token 需加密）
- 无索引：metrics(run_id)、checkpoints(run_id) 全表扫
- 设置"空值删除"注释与实现不符（system.rs:288 vs 299-301 continue）
- 设置值未校验注入内核 env；且 HF_ENDPOINT/PIP_INDEX_URL 被强制 `HF_HUB_OFFLINE=1`/`TRANSFORMERS_OFFLINE=1` 抵消（功能失效 + 文档漂移）
- delete_run 只删 runs/<id>，output/<id> 产物目录残留累积；remove_dir_all 错误静默
- 无安全头（X-Content-Type-Options/CSP/Referrer-Policy）；SSE 连接数无上限
- 端口回退 u16 溢出（lib.rs:157 `config.port + offset`）；`--web` 浏览器探测只查 port/port+1（cli main.rs:264-267）
- cancel 超时仅 5s（文档 30s）；超时后死句柄滞留（handles.remove 在 ? 之后）；cancel 里同步 taskkill 阻塞 async 线程
- CLI kernel install 用 `run(...).expect(...)` 失败即 panic（24 unwrap + 23 expect 集中在 cli/kernel.rs）；步骤编号 [5/6]→[6/7]→[7/7] 不一致
- `lr_warmup_steps` 硬编码 `epochs × 1000` 估算（toml_map.rs:489-492），小数据集可能超过总步数
- validate 漏网：dropout 范围、block_weights 格式、max_token_length 集合、min_timestep≤max_timestep、max_grad_norm>0、num_repeats≥1 均未校验；hash_threshold 未限 [0,64]；target_resolution=0 无兜底 → 桶候选集为空
- walkdir 错误静默丢弃（scan.rs:107 filter_map ok()）；根目录不存在时静默返回空结果；符号链接图片被跳过（follow_links(false) 的副作用）
- 非 UTF-8 文件名 `to_string_lossy` 坍缩碰撞（Linux 源目录场景）
- Supervisor 持锁 IO（B4）、detect_resume_dir 同步递归大目录阻塞 async 线程
- EventBus::new(0) 无断言；`ev_run_id` 与 `event_run_id` 双份重复实现；`now()` 工具 7+ 处重复
- 未知 `/api/*` 404 空体与统一 `{"error"}` 形态不一致；内核非 JSON 行被静默丢弃（UI 日志流看不到 tqdm 输出，架构承诺"降级 log 事件"未实现）
- **TOML/YAML 字符串值不转义**（引号/反斜杠直插：yaml_map.rs:163-166 prompt、toml_map.rs 各处 format!）——配方值可注入配置键（CORS 放开时远程可构造），参数注入面
- **steps 估算未除 batch_size**（compat lib.rs:151-154 = 图片数×epochs×repeats；若修复 P0-4 的 batch_size 映射需同步修正公式）
- **POSIX kill-tree 只杀 wrapper 单进程**（kernel.rs:59 `kill -TERM <pid>`，无 killpg——与 kernel_runner.py 的 POSIX killpg 分支不一致）
- **doctor torch 探测无超时**（doctor.rs:94-102 `.output()` 无 timeout，venv import torch 卡住时 doctor 永久挂起）
- 仓库卫生：bat GBK 编码、21MB PNG 在 git 历史、一次性脚本散落 .kernel-ws（§7）

---

## 4. 前端与 UX 问题清单

### 安全（已确认无问题 ✅）
- **无 XSS 面**：全目录 `dangerouslySetInnerHTML`/`innerHTML` 0 处；内核日志按 React 文本节点渲染，自动转义。
- 破坏性操作（删任务/清空/删产物/删丹方/训练中取消）全部有 confirm；图片懒加载。
- 主要安全面在服务端（CORS/路径遍历，见 P0-2/P0-3），UI 无法自保。

### 🟠 P1

| # | 问题 | 证据 |
|---|---|---|
| U1 | **自动命名丹方首次点火必失败**：`setName()` 异步生效，`onSave()` 读本次渲染闭包的旧 name（空）→ 提示"请填写丹方名称" | TrainSetup.tsx:697-702 vs 655-658 |
| U2 | **保存丹方=删旧建新**：新丹方校验失败则旧丹方已永久丢失；delete 失败则产生重复 | TrainSetup.tsx:663-670 |
| U3 | **载入丹方后改参数直接点火被静默忽略**：有 recipeId 时 onFire 直接用旧 rid，不重建 payload → 训练用旧参数 | TrainSetup.tsx:697-702 |
| U4 | **失败原因 UI 不可见**：Console.tsx:69-78 只收 log 事件，`Event::Fail.tail`（含"内核启动失败"等）被丢弃；使用指南 Q3"看日志尾部报错"无法兑现 | Console.tsx:69-78、queue.rs:204-208 |
| U5 | **SSE 每事件全量刷新风暴**：App.tsx:112 每条事件 refreshRuns（=listRuns+fetchRunPreviews 两个 HTTP）；Console.tsx:128-130 sample 后每条事件再 listCheckpoints；mock 250ms 一条 progress → 每秒 ~8+ 请求 | App.tsx:104-113、Console.tsx:128-130 |

### 🟡 P2（节选，完整清单见报告文件）
- SSE 无 onerror/onopen：断线无提示、顶部"已点火"过期不更新、服务换端口不重连（api.ts:94-98）
- 连接失败无限重试，"正在点火…"永不落错误态（App.tsx:84-101）
- 续丹/取消后整页 reload 丢视图（Console.tsx:183,194）
- 运行中任务也显示删除按钮（必 409）；任务行 li 无键盘可达性；✕ 无 aria-label；进度环无 role/aria
- running 与 failed 状态同为朱砂红仅亮度 0.85 区分；--ink-dim 对比度 4.3:1 不足 4.5:1
- 日志强制滚底覆盖用户上翻；GPU 轮询后台标签页照跑
- LossCurve 首点 loss=null 时 SVG 路径以 L 开头（从 (0,0) 画杂线）；`.run` 4 子元素网格换行
- assetUrl 未 encodeURIComponent（文件名含 #/? 时 404）；listRuns/Console 数据加载失败静默
- 丹方表单无必填标记、错误不定位到字段；成功/失败消息同色（琥珀金）
- 双击点火可能创建重复任务（busy 异步生效）
- 连续点火失败/部分失败无反馈（清空记录单条失败被吞）

### UX 亮点（保留）
炼丹隐喻贯彻良好（点火/熄火/文火/炸炉/出炉/丹方）；深色琉璃主题与 PRD §9.1 色板一致；空状态与加载态文案齐全；图片懒加载；状态清理（cancelled 标志、es.close）正确，未发现监听泄漏/重复订阅。

---

## 5. 安全风险总览（攻击面视角）

| 风险 | 严重度 | 利用前提 | 位置 |
|---|---|---|---|
| CORS 全放行 → 任意网页读写本地服务 | 🔴 P0 | 用户浏览任意网页（常驻） | server lib.rs:63 |
| run_logs 路径遍历任意文件读 | 🔴 P0 | 同上（读文件尾 300 行） | vault.rs:27-47 |
| 产物删除/重命名出界（cp.path 污染） | 🟠 P1 | 内核事件被污染/恶意数据集 | supervisor.rs:129-134 + vault.rs |
| 丹方名路径分隔符 → 产物写出目录 | 🟠 P1 | 创建恶意丹方名（CORS 下远程可触发） | recipes.rs + queue.rs:91 + compat lib.rs:224-228 |
| import-asset 任意文件收纳 + 对话框路径回传 | 🟠 P1 | CORS 放开时 | system.rs:38-86,177-226 |
| 设置值未校验注入内核 env | 🟡 P2 | 本地 | queue.rs:122-130 |
| 无安全头 / SSE 无限连接 / scan DoS | 🟡 P2 | CORS 放开时 | lib.rs、sse.rs、datasets.rs |
| SQL 注入 | ✅ 无 | — | 全部参数化 |
| 命令注入 | ✅ 无 | — | nvidia-smi/内核命令/taskkill 参数固定 |
| 静态服务路径穿越 | ✅ 无 | — | tower-http ServeDir 规范化 |
| 前端 XSS | ✅ 无 | — | 0 处 innerHTML |
| 依赖已知漏洞 | ✅ 无 | — | cargo audit / npm audit 0 漏洞 |

**威胁模型说明**：本项目定位"本地单用户无鉴权"，绑定 127.0.0.1 本身合理；但 P0-2/P0-3 组合使**浏览器恶意网页成为有效攻击者**，这与 PRD §7"仅监听 127.0.0.1 即安全"的假设不符，是最需要优先修复的面。

---

## 6. 文档漂移汇总（文档 vs 代码）

| 文档 | 声称 | 实际 |
|---|---|---|
| README.md | "M0 完成，M1 未开始" | M1/M2/M3 全部完成（roadmap + git log） |
| 使用指南 §四 | 3 页签：炼丹/药材/炉房 | 实际：LoRA 训练 / 全量训练 / 炼丹记录；**药材页、炉房页不存在** |
| 使用指南 §四 | 点火=创建**模拟**训练 | 真实入队训练（simulate 仅 curl 用，UI 从不调用） |
| 使用指南 §五 | 预设套用 / 点扫描 / 注册表单 / 新建炼丹向导 | 全部不存在（并入丹方表单） |
| 使用指南 §六 | `tiandi kernel install-aitk` 等命令 | CLI 有，一致 ✅ |
| architecture §5.3/5.4 | pause/resume 命令；心跳 30s 看门狗；hello 10s 握手校验；Job Object | **均未实现**（taskkill /T /F；无心跳；Unsupported） |
| architecture §5.2 | done 含 artifacts；metric 含 name/value；非 JSON 行降级 log 事件 | done={code}；metric={step,loss,lr}；非 JSON 行被丢弃 |
| architecture §3 | BaseModel 含 type/vae/text_encoders/version；Metric 含 epoch；LogEntry 类型 | 仅 path/sha256/source；无 epoch；LogEntry 不存在 |
| architecture §3/PRD §8.5 | manifest.json 崩溃恢复 | 从未接线（manifest_path 恒 None） |
| architecture §6 | 丹方校验拒绝未知键 | 未知键静默接受 |
| architecture §7 | 缩略图 AVIF/WebP | 仅 JPEG |
| architecture §7 | 异步 + rayon 并行 | 同步扫描 + spawn_blocking 包装（表述不精确） |
| architecture.md:115 | 日志分片滚动 | 无限追加 |
| PRD FR-102 | BaseModel 路径校验/版本/来源 | 无校验、无 version 字段 |
| PRD FR-202/205 | 尺寸过滤/模糊检测/标签频次统计 | 未实现 |
| PRD FR-401 | 时间步采样策略/flow 预测目标 | RecipeData 无这些字段（Krea2 flowmatch 硬编码） |
| architecture §3 | Run 含参数快照/产物引用 | Run 结构无 params/artifacts 字段（仅 RunManifest.params，且未接线） |
| PRD FR-504 | created→queued→… 迁移链 | 代码额外允许 Created→Failed（设计扩张，文档未注明） |
| 使用指南 §六 | 常用命令列表 | 漏了 `tiandi server` 命令（§141-155 只列 init/doctor/kernel/models） |
| PRD FR-502 | 兼容引擎"全参数覆盖" | 实际仅 M1 参数子集（optimizer/scheduler 等部分未映射）——过度声称 |
| roadmap M2 | "T-LoRA 走 lora_anima" | 与 toml_map.rs:60-62 的 `networks.tlora_anima` 矛盾（见 P0-6） |
| PRD FR-701/FR-702 | 队列/通知/自动化 CLI `tiandi run` | 队列有；`tiandi run` 子命令不存在 |
| PRD FR-801/802 | loss/lr 曲线（有 ✅）；磁盘余量 | 磁盘余量未实现 |
| README curl 冒烟 `?simulate=1` | — | ✅ 可用（无漂移） |
| 端口回退 18765-18774 + UI 探测 | — | ✅ 已实现 |

---

## 7. 仓库卫生

1. **21 MB 测试 PNG 已进入 git 历史**（`测试数据集/1_tag/*.png`，commit c7acbb5；现已从工作区删除，历史仍在，.git 现有 ~12MB loose 对象）。建议 git filter-repo 清理或接受。
2. `测试底模/`、`测试数据集/` 目录本身无 .gitignore 条目（靠文件类型忽略），放非 safetensors 文件会被跟踪。
3. `.kernel-ws` 是运行时数据目录却位于仓库内（已忽略，10.6 GB 磁盘占用）；13 个一次性 Python 脚本 + panic.log 散落其中，无版本管理。
4. `start.bat`/`server_window.bat` 为 **GBK 编码**且 server_window.bat 存在 `chcp 65001` 与 GBK 内容自相矛盾（窗口中文乱码）；建议转 UTF-8 并统一。
5. `target/` 18.6 GB、`.kernel-ws` 10.6 GB、`测试底模` 72.7 GB —— 如需备份/迁移注意体积。
6. 文档数字过时：README"31 测试"、roadmap"70 测试"（实测 74）；README 状态表过时（见 §6）。
7. `cargo fmt` 未通过（建议加 CI 门禁）；cargo-machete 发现的 7 项未使用依赖可清理。

---

## 8. 修复优先级总表

### 立即（P0）
1. 迁移包事务 + v3 幂等（tiandi-state/src/lib.rs:32-138）
2. CORS Origin 白名单/CSRF token（tiandi-server/src/lib.rs:63）
3. run_logs 加 get_run 校验 + canonicalize 前缀断言（vault.rs:27-47）
4. sd-scripts TOML 映射输出 batch_size（toml_map.rs）
5. 缩略图 temp+rename 原子写（scan.rs:142-144 / thumb.rs）
6. Anima T-LoRA 映射修正（networks.tlora_anima 不存在，toml_map.rs:60-62 → lora_anima）
7. disable_sampling 移到 ai-toolkit train 段（yaml_map.rs:169-171）
8. krea2_prepare.py 输出改 UTF-8 直写 + 修复当前 kernel.json 乱码路径（当前 Krea2 训练已坏）

### 尽快（P1）
9. 队列原子认领（置 Preparing + 锁）杜绝重复/双拉起（queue.rs:27-67）
10. cancel 语义修正（Queued 直接取消；强杀后以 Canceled 为准）
11. 崩溃恢复补 Paused；启动清理孤儿内核；关停杀 handles（Job Object 或遍历 kill）
12. Mutex poison 处理（compat lib.rs:334/341/410）
13. 状态机事实源统一（Failed 终态 vs 重试）
14. 前端 U1-U5（自动命名闭包、保存丹方顺序、点火前保存校验、Fail.tail 渲染、SSE 节流）
15. dHash 全零排除 + 聚类修正；EXIF Orientation；解码上限
16. 未知键策略（deny_unknown_fields 或 Warning）；Krea2 映射消费丹方字段
17. supervisor 文件 IO 移出 store 锁 + spawn_blocking；日志旋转 + run_logs 尾读
18. 产物路径统一规范化；丹方名禁路径分隔符
19. 真实训练补 sample 事件（内核侧或文件监控）；资产路径脱离 CARGO_MANIFEST_DIR（分发可用）
20. CLI 工作区 canonicalize；统一 Tauri 壳与浏览器模式工作区

### 建议（P2）
21. 清理全部死代码/死端点/未使用依赖（§2 清单，含 ready/env_message/kernel_env、anima_qwen3_tokenizer、--mock 死分支、seed_tokenizers.py）；补 metrics/checkpoints 索引；多语句删除包事务；busy_timeout/WAL
22. 文档同步（README 状态、使用指南三页签+补 server 命令、architecture §5 承诺项删承诺或实现、FR-502 全参数覆盖改口径、roadmap T-LoRA 说法）
23. UI 可访问性/状态色/对比度/断线提示/错误态（§4 P2 清单）
24. 仓库卫生（bat 编码、git 历史清理、gitignore 补目录条目）
25. 补测试：迁移崩溃窗口、未知键、纯色图判重、跨前缀近重复、batch_size 映射、EXIF 旋转、tlora_anima 映射、disable_sampling 层级、边界参数

---

---

## 10. 修复记录（2026 全量修复轮，已全部落地并验证）

> 本轮修复由 7 路并行子代理 + 人工完成，全部 P0 与主要 P1 已修复。验证：**cargo test --workspace 132 全绿**（修复前 74）、**clippy 0 警告**、**cargo fmt 通过**、**cargo machete 干净**（kamadak-exif 误报已声明忽略）、**npm run build 通过**。

### 已修复清单

| 编号 | 问题 | 修复 |
|---|---|---|
| P0-1 | SQLite v3 迁移非幂等 | migrate 每版本块 `BEGIN IMMEDIATE` 事务 + v3 条件 ALTER（column_exists 检查）+ 崩溃窗口测试 |
| P0-2 | CORS 全放行 | `guard_local_access` 中间件：Host 白名单（防 DNS rebinding）+ Origin 白名单（localhost/127.0.0.1/tauri），违规 403；另加 nosniff 头 |
| P0-3 | run_logs 路径遍历 | get_run 校验 + canonicalize 越界 400 + 64KB 尾部读取 300 行 |
| P0-4 | batch_size 静默丢失 | `[training] train_batch_size`（对照已安装 sd-scripts 实测键名）+ 测试 |
| P0-5 | 缩略图并行写竞争 | temp+rename 原子写（唯一临时名含 pid+线程 id）+ 残留清理 |
| P0-6 | Anima T-LoRA 必崩 | `networks.tlora_anima` → `networks.lora_anima`（实测 068bcd7 无 tlora 模块） |
| P0-7 | disable_sampling 层级错误 | 移到 ai-toolkit `train:` 段（实测 config_modules/SDTrainer 消费处） |
| P0-8 | krea2 中文路径乱码 | krea2_prepare.py UTF-8 字节直写 + Rust 严格解码警告 + **修复当前 kernel.json**（mmdit 已恢复 `D:/Projects/tiandi/测试底模/krea2/krea2_raw_bf16.safetensors`） |
| P1-1 | Mutex poison panic | `lock().unwrap_or_else(\|e\| e.into_inner())` |
| P1-2 | 队列重复/双拉起 | `Store::claim_next_queued()` BEGIN IMMEDIATE 事务原子认领（置 Preparing） |
| P1-3 | cancel 语义 | Queued/Created 直接 Canceled；运行中先置 Canceled 再终止内核；EOF 兜底 fail 不覆盖 |
| P1-4 | 崩溃恢复漏 Paused + 孤儿进程 | recover 补 Paused；优雅关停 `kill_all()`；POSIX 进程组杀（killpg + process_group(0)） |
| P1-5 | 状态机事实源分裂 | `Failed → Queued` 成为合法迁移（用户重试），事件流仍终态；文档同步 |
| P1-6 | broadcast 丢事件/持锁 IO | 容量 1024→4096；supervisor 文件 IO 全部移出 store 锁 |
| P1-7 | manifest 未接线 | 保留 RunManifest（记录于遗留项） |
| P1-8 | 旧格式 state 队列不可见 | 查询同时匹配带引号变体 |
| P1-9 | dHash 误判/漏检/非传递 | 全零哈希跳过 + 4×17 多索引精确候选（鸽笼零漏检）+ 并查集传递闭包 |
| P1-10 | EXIF Orientation 未应用 | Orientation 1-8 全处理（含镜像），旋转后取宽高/哈希/缩略图 |
| P1-11 | 未知键静默 | RecipeData 增加 5 个前端自定义字段（model/vae/te/dataset_dir/full_finetune），保持 permissive |
| P1-12 | Krea2 映射硬编码 | optimizer/lr_scheduler/sample_sampler/seed/cache_latents 消费丹方字段（带回退+注释） |
| P1-13 | 心跳/卡死检测未实现 | 内核 2s heartbeat + Rust 30s 看门狗自动 kill（已实现并去重） |
| P1-14 | 产物路径越界 | supervisor 越界拒绝入库；vault 删除/重命名 canonicalize 断言 |
| P1-15 | 日志无旋转/整读 | 5MB 轮转 .1；run_logs 尾部 64KB 读取 |
| P1-16 | 解码无上限/两次解码 | image_dimensions 头部检查（宽/高 16384、6400 万像素上限）+ 单次解码复用 |
| P1-17 | 前端 U1-U5 | 自动命名闭包、先建后删、点火前必保存、Fail.tail 红显、SSE 1s 防抖 |
| P1-18 | 真实训练无采样事件 | kernel_runner 采样目录监控线程（2s 轮询，绝对路径，去重） |
| P1-19 | 资产路径依赖源码树 | `asset_path()`（exe 目录 → exe/assets → CARGO_MANIFEST_DIR 兜底），server/cli 接入 |
| P1-20 | CLI 不 canonicalize | resolve_dir canonicalize；Tauri 壳工作区差异记入遗留项 |
| P2 | 死代码/依赖/事务/索引等 | 未使用依赖 7 项全清、delete_* 事务化、metrics/checkpoints 索引、busy_timeout+WAL、设置空值删除、端口 saturating、delete_run 清理 output、字符串转义（TOML/YAML）、validate 10 条补全、doctor 超时、UI 可访问性/断线重连/状态色 |

### 遗留项（本轮未做，见 §8 P2 清单）

- hello 10s 握手强制校验、pause/resume 训练侧实现、Windows Job Object（现为 taskkill）
- RunManifest 崩溃恢复链路接线（现为 DB 状态扫描恢复）
- Tauri 壳与浏览器模式工作区统一（app_data_dir vs .kernel-ws）
- 死代码清理：RunStateError、Store::conn()、ModelFamily::backend()、Run::manifest_path、family_supported、label()、ScanError::new、ready/env_message/kernel_env、anima_qwen3_tokenizer、--mock 死分支、seed_tokenizers.py、CSS 死规则、STATE_LABEL 双份、无客户端端点（projects/transition/images/buckets/validate/presets/checkpoints 列表）
- 前端 P2 细节：GPU 轮询 visibilitychange、日志滚底策略、LossCurve 首点 null、任务行缩略图换行、双击点火去重、清空记录失败反馈、CSS 对比度/焦点样式
- 仓库卫生：git 历史 21MB PNG、bat 编码、gitignore 补目录条目

---

## 9. 审查覆盖说明

- 后端：10 个 crate 全部源码逐行审查（core/state/server/recipe/dataset 由子代理 + 人工复核；engine/compat/cli/app 由本人逐行阅读，Python 资产 kernel_runner.py / krea2_prepare.py / seed_tokenizers.py 全部阅读，并对照已安装内核实测：`.kernel-ws/.kernel/sd-scripts@068bcd7` 的 networks/ 目录、`.kernel-aitk/ai-toolkit` 的 config_modules/SDTrainer 源码、`kernel.json` 实况）。
- 前端：全部 TS/TSX/CSS 逐行 + UX 对照 PRD §9 与使用指南。
- 文档：PRD / architecture / roadmap / model-support / 使用指南 全部阅读并与代码对照。
- 工具：clippy / test / fmt / machete / audit（Rust+NPM）/ tsc / vite build 全部实测。
- 未运行真实训练（需 GPU + 内核），但基于 `.kernel-ws` 真实运行产物（tiandi.db、panic.log、runs 目录）交叉验证了事故与运行痕迹。
